'use strict';
import assert from 'assert';

import { numeros, strings, booleanos, mezcla, objetos } from './data.js';

// EJERCICIOS
// Sustituye el "null" por lo que sea necesario en cada caso

// Se accede al quinto elemento del array "booleanos"
const resultado1 = null;
assert.equal(resultado1, false);

// Se accede al cuarto elemento que está en "numeros"
const resultado2 = null;
assert.equal(resultado2, 67);

// Se accede al sexto elemento del array "mezcla"
const resultado3 = null;
assert.equal(resultado3, 565);

// Se accede a la talla del segundo objeto en el array "objetos"
const resultado4 = null;
assert.equal(resultado4, 'xl');

console.log('¡ENHORABUENA TODO CORRECTO!');
